# Rename to secrets.toml locally; on Streamlit Cloud paste this into
# App Settings → Secrets. This file is git-ignored so real secrets never
# reach GitHub.
admin_pin = "change-me-to-something-long"
